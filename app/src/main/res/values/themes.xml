<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Always black, regardless of system light/dark setting - the splash/window background
         before Compose (QMetronomeTheme) takes over, which is the actual source of truth. -->
    <style name="Theme.Qmetronome" parent="Theme.MaterialComponents.NoActionBar">
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/qm_navy</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="colorSecondary">@color/mid_gray</item>
        <item name="colorSecondaryVariant">@color/unlit_gray</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:windowBackground">@color/black</item>
        <item name="android:statusBarColor">@color/black</item>
    </style>
</resources>
